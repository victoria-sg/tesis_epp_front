diff a/front/src/views/admin/UsuariosView.tsx b/front/src/views/admin/UsuariosView.tsx	(rejected hunks)
@@ -19,7 +19,7 @@ import type {
 } from "../../models/usuario.model";
 import { usuarioService } from "../../services/usuario.service";
 import {
-  usuarioSchema,
+  buildUsuarioSchema,
   type UsuarioFormValues,
 } from "../../validators/usuario.schema";
 
@@ -29,8 +29,17 @@ const INITIAL_VALUES: UsuarioFormValues = {
   correo: "",
   id_rol: 0,
   contrasena: "",
+  confirmar_contrasena: "",
 };
 
+// No se incluye "Administrador": un administrador no puede asignar el
+// rol de Administrador a otro usuario desde este formulario (la API
+// también lo rechaza, esto solo evita el viaje de ida y vuelta al backend).
+const ROLES_ASIGNABLES = [
+  { value: 2, label: "Supervisor de Turno" },
+  { value: 3, label: "Encargado SSO" },
+];
+
 const FIELD_MAPPING: Record<string, string> = {
   nombre: "nombre",
   apelido: "apelido",
@@ -45,6 +54,11 @@ export const UsuariosView = () => {
     pageSize: PAGE_SIZE,
   });
 
+  const usuarioSchema = useMemo(
+    () => buildUsuarioSchema(crud.isEditing),
+    [crud.isEditing],
+  );
+
   const { formik, handleSubmit: handleFormSubmit } =
     useCrudForm<UsuarioFormValues>({
       isEditing: crud.isEditing,
@@ -56,7 +70,6 @@ export const UsuariosView = () => {
       initialValues: INITIAL_VALUES,
       fieldMapping: FIELD_MAPPING,
       onSubmit: async (values) => {
-        const { isEdit } = values as { isEdit?: boolean };
         if (crud.isEditing) {
           const updateData: UsuarioUpdate = {
             nombre: values.nombre,
@@ -227,27 +240,42 @@ export const UsuariosView = () => {
             label="Rol"
             value={formik.values.id_rol}
             onChange={(v) => formik.setFieldValue("id_rol", v)}
-            options={[
-              { value: 1, label: "Administrador" },
-              { value: 2, label: "Supervisor de Turno" },
-              { value: 3, label: "Encargado SSO" },
-            ]}
+            options={ROLES_ASIGNABLES}
             placeholder="Seleccionar rol…"
             error={formik.errors.id_rol}
             touched={formik.touched.id_rol as boolean | undefined}
           />
           {!crud.isEditing && (
-            <CustomInput
-              label="Contraseña"
-              name="contrasena"
-              type="password"
-              placeholder="••••••••"
-              value={formik.values.contrasena ?? ""}
-              onChange={formik.handleChange}
-              onBlur={formik.handleBlur}
-              error={formik.errors.contrasena}
-              touched={formik.touched.contrasena as boolean | undefined}
-            />
+            <>
+              <CustomInput
+                label="Contraseña"
+                name="contrasena"
+                type="password"
+                placeholder="••••••••"
+                value={formik.values.contrasena ?? ""}
+                onChange={formik.handleChange}
+                onBlur={formik.handleBlur}
+                error={formik.errors.contrasena}
+                touched={formik.touched.contrasena as boolean | undefined}
+              />
+              <CustomInput
+                label="Confirmar contraseña"
+                name="confirmar_contrasena"
+                type="password"
+                placeholder="••••••••"
+                value={formik.values.confirmar_contrasena ?? ""}
+                onChange={formik.handleChange}
+                onBlur={formik.handleBlur}
+                error={formik.errors.confirmar_contrasena}
+                touched={
+                  formik.touched.confirmar_contrasena as boolean | undefined
+                }
+              />
+              <div className="text-[11px] text-[#6b6b6b] -mt-1">
+                Mínimo 8 caracteres, con mayúscula, minúscula, número y
+                carácter especial.
+              </div>
+            </>
           )}
           {crud.error && (
             <div className="text-[12px] text-red-600 text-center">
